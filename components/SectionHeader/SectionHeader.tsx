import React from 'react'

const SectionHeader = ({title}:any) => {
  return (
   
    <h6 className='section-header uppercase font-[600] tracking-[1px] font-syncopate text-[#626262]'>
        {title}
    </h6>
  )
}

export default SectionHeader